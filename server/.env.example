# DecisionMind Backend Environment
PORT=5001
NODE_ENV=development

# Optional: Google Gemini API Key
# If omitted, server falls back to high-fidelity offline consensus engine
GEMINI_API_KEY=your_gemini_api_key_here
